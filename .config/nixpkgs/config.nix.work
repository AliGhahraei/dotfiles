{
  allowUnfree = true;
  packageOverrides = pkgs: with pkgs; {
    myPackages = pkgs.buildEnv {
      name = "my-packages";
      paths = [
        awscli
        azure-cli
        pgadmin4
        onlyoffice-bin
        mongodb-compass
        sioyek
      ];
    };
  };
}
